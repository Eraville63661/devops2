name: Python Application Workflow 3

# on: [schedule]:
#   - cron: '5 0 0 0 0'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'
